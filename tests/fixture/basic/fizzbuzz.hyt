import { println } from "std/io";

fn fizz_buzz(n: Int) {
    match (n % 3, n % 5) {
        (0, 0) => println("fizzbuzz"),
        (0, _) => println("fizz"),
        (_, 0) => println("buzz"),
        (_, _) => println(n),
    }
}
