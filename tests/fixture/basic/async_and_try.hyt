@MockNetwork;
module Network {
    pub struct Response {
        final code: Int;
        final body: String;
    }

    pub extern async fn fetch(url: String) : Response;

    async fn handle_request(url: String) : Void {
        let resp = try await fetch(url);
        
        match resp {
            (200, b) => {
                print("Success: " + b);
            }
            (n, _ ) => {
                print("Server Error");
            }
            _ => print("Unknown error");
        }
    }

    fn main() {
        ignore await handle_request("https://api.example.com");
    }
}
